# MVC Architecture

Variant: Classic MVC API.

Folders are grouped by technical role (`controllers`, `routes`, `models`, `services`, `repositories`).
This scaffold intentionally avoids Clean/Hexagonal layering.
